// === - equality operator
// !== - not equal operator 
// < - less than operator
// > - greater than operator
// <= - less than or equal to operator
// >= - greater than or eqaul to operator

let temp = 180

if (temp <= 32) {
    console.log('It is freezing outside!')
}

if (temp >= 110) {
    console.log('It is way too hot outside!')
}

// Challenge area

let age = 6

if (age <= 7) {
    console.log('Congrats you qualify for discount child pricing!')
}

if (age >= 65) {
    console.log('You receive disounted senior pricing!')
}

// If 7 or under print message about child pricin

// if 65 or older print message about senior discount

